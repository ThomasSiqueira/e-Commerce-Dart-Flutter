import 'package:flutter/material.dart';

class UpAppBar extends StatelessWidget {
  const UpAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('tittle test'),
      ),
    );
  }
}
